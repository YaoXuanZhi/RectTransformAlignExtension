# RectTransform Align Extension

本插件参考自ThunderFireUIXTool，解决了该插件某些情况下Prefab 浮窗UI元素重叠的问题，并且完全采用IMGUI的方式来重写插件UI

## 📦 安装方法

### 通过 Unity Package Manager

1. 打开 Unity Package Manager（窗口 > Package Manager）
2. 点击左上角的"+"按钮
3. 选择"Add package from disk..."
4. 导航到包文件夹并选择 `package.json`

### 手动安装

1. 将整个包文件夹复制到项目的 `Packages` 目录
2. Unity 将自动检测并导入该包